class CurrentProduct < ApplicationRecord
    #model has mapping for key (product title), product_id, next_month_product_id, and if prepaid product
    #prepaids don't have anything but product_collection updated
end
